spring:
  application:
    name: gulimail-coupon

  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        # namespace: 指定的是命名空间的uuid 默认的就是使用public的
        #使用命名空间进行环境的隔离  使用微服务之间的进行环境的隔离
        # group: 指定的是配置集分组 默认的是default group
        # 使用的是每一个微服务创建自己的命名空间  使用配置分组区分环境
        # ext-config:
         # - dtasource.yml
         # - 配置多数据集


#        ext-config:
#          - {data.id: }


